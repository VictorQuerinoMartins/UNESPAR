// Verificar erros: "ferror()"

// Voltar o cursor ao início do arquivo: "rewind()"
// Exemplo tem um valaor de X e Y, sem rewind ele pegaria o getsc da primeira posição do cursor ou seja X, e dps automáticamente Y, usando rewind ele pegaria X "rewind" e dps voltaria para X.

// Para apagar o arquivo ou seja o .txt: "remove"

// Trocar o nome do arquivo: "rename"